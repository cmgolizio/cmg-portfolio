// Placeholder body for the /work-in-progress route
// its only job is to display the message it's handed, so any not ready section can reuse it.
export default function WorkInProgress({ message }) {
  return <h1 className='reveal'>{message}</h1>;
}
